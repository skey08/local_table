<h3>All Goods:</h3>
<ul>
  <% @goods.each do |good| %>
    <ul>
      <%= link_to "#{good.make} - $#{good.price}",carpenter_good_path(good.carpenter,good) %> <br>
      <img class = 'good-photo' src="<%= good.photo_url %>">
    </ul>
  <% end %>
</ul>
<%= link_to 'Back', carpenters_path %>
